---

spring:
  application:
    name: attendee-viewer
  cloud:
    config:
      uri: ${vcap.services.config-server.credentials.uri}
      username: timandgerard
      password: herpderp
    
management:
  security:
    enabled: false